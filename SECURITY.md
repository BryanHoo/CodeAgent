# 安全策略

请勿通过公开 Issue 披露漏洞、Token、Prompt、命令输出、文件内容或本地路径。请使用仓库托管平台的私密安全报告功能联系维护者。

当前项目处于架构阶段，尚未提供可部署版本。安全实现必须遵守以下默认边界：

- 服务只监听 Loopback 地址。
- 浏览器不直接连接 Codex App Server 或本地文件系统。
- Secret 不进入命令行参数、日志、Web 响应和仓库。
- Project 路径、审批和 Provider 请求必须在服务端逐次校验。
- 所有子进程、RPC、数据库和关闭流程必须设置超时。
