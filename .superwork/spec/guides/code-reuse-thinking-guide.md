# 代码复用检查指南

## Goal

在不破坏模块边界的前提下减少重复实现。

## Checklist

- 先在当前包内搜索已有入口、类型和工具，再扩大到整个 Workspace。
- 通用协议放入 `packages/protocol`，领域规则放入 `packages/core`，不要用共享工具包掩盖归属问题。
- 只有两个以上真实消费者需要同一实现时才提取复用层。
- 新公共入口统一从包根 `src/index.ts` 导出，禁止直接导入其他包的内部路径。
