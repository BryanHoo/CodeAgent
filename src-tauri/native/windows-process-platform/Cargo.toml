[package]
name = "windows-process-platform"
version = "0.1.0"
edition = "2024"
publish = false

[target.'cfg(windows)'.dependencies]
windows = { version = "0.61.3", features = [
  "Win32_System_LibraryLoader",
] }
